# Каталог служебный: по HTTP он не должен открываться ни одним файлом.
# Здесь лежат пароль базы, сама база и внутренний код CMS.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>

<?php
/**
 * Единая конфигурация сайта.
 *
 * ВАЖНО: значения null означают «данные не предоставлены владельцем».
 * Сайт продолжает работать, но такие данные не выводятся в текстах,
 * не попадают в микроразметку и перечислены в DATA_REQUIRED.md.
 *
 * Ничего не выдумывайте в этом файле: ИНН, ОГРН, стаж, количество объектов
 * и гарантийные сроки должны быть реальными.
 */

return [

    // ── Бренд и позиционирование ──────────────────────────────────────────
    'brand' => [
        // Рабочее название. ЗАМЕНИТЕ на утверждённое название компании.
        'name'         => 'КОНТУР',
        'name_full'    => 'КОНТУР — натяжные потолки',
        // Короткое позиционирование, используется в шапке и микроразметке.
        'positioning'  => 'Проектирование, изготовление и монтаж натяжных потолков',
        'tagline'      => 'Потолок как часть архитектуры интерьера',
    ],

    // ── Адрес сайта ───────────────────────────────────────────────────────
    'site' => [
        'scheme'      => 'https',
        // ЗАМЕНИТЕ на реальный домен без слэша в конце.
        'domain'      => 'example-potolki.ru',
        // Подкаталог, если сайт стоит не в корне домена: '' или '/potolki'.
        'base_path'   => '',
        'locale'      => 'ru_RU',
        'timezone'    => 'Europe/Moscow',
        // Каноническая версия домена: 'www' | 'non-www' | null (не трогать)
        'canonical_host' => null,
        'force_https' => false,
    ],

    // ── Контакты ──────────────────────────────────────────────────────────
    'contacts' => [
        // ЗАМЕНИТЕ на рабочие номера. Формат display — как показывать,
        // raw — как подставлять в tel: (только цифры и плюс).
        'phone'            => ['display' => '+7 (000) 000-00-00', 'raw' => '+70000000000'],
        'phone_secondary'  => null,
        'email'            => 'info@example-potolki.ru',
        // Адрес получателя заявок (может отличаться от публичного email).
        'email_leads'      => 'info@example-potolki.ru',
        // Мессенджеры: null — кнопка не показывается.
        'whatsapp'         => null,   // '+70000000000'
        'telegram'         => null,   // 'https://t.me/username'
        'max'              => null,   // ссылка на профиль в MAX
        'work_hours'       => 'Ежедневно 09:00–21:00',
        'work_hours_schema'=> [['days' => ['Mo','Tu','We','Th','Fr','Sa','Su'], 'open' => '09:00', 'close' => '21:00']],
        // Публичный адрес офиса/шоурума. null — если офиса для приёма клиентов нет.
        // Не пишите адрес, по которому нельзя приехать: это вводит в заблуждение.
        'office_address'   => null,
        'map_url'          => null,   // ссылка на карточку в Яндекс.Картах
        'map_embed'        => null,   // HTML-код виджета карты (вставляется как есть)
        'coords'           => null,   // ['lat' => 55.75, 'lon' => 37.62]
    ],

    // ── Юридические данные оператора персональных данных ──────────────────
    // Заполняются владельцем. Пока пусты — страница /legal/requisites/
    // показывает предупреждение, а микроразметка Organization не выводит реквизиты.
    'legal' => [
        'operator_name'   => null,   // 'ООО «...»' или 'ИП Фамилия И. О.'
        'inn'             => null,
        'kpp'             => null,
        'ogrn'            => null,   // или ОГРНИП
        'legal_address'   => null,
        'postal_address'  => null,
        'privacy_email'   => null,   // адрес для обращений по персональным данным
        // Уведомление Роскомнадзора об обработке персональных данных подано?
        'rkn_notified'    => null,   // true | false | null (не проверено)
        'data_storage_ru' => null,   // где физически хранятся данные (хостинг в РФ)
    ],

    // ── География работы ──────────────────────────────────────────────────
    'geo' => [
        'base_city'        => 'Москва',
        'region'           => 'Московская область',
        'region_short'     => 'Подмосковье',
        // Бесплатная зона выезда замерщика, км от МКАД.
        'free_travel_km'   => 30,
        // Показывать ли раздел /metro/ (страницы по станциям метро).
        'metro_enabled'    => false,
    ],

    // ── Гарантии ──────────────────────────────────────────────────────────
    // ВНИМАНИЕ: не публикуйте сроки, которых нет в договоре.
    'guarantee' => [
        'canvas_years'  => null,   // гарантия на полотно, лет
        'works_years'   => null,   // гарантия на монтажные работы, лет
        'lighting_years'=> null,   // гарантия на светотехнику, лет
        'notes'         => null,   // краткое пояснение условий
    ],

    // ── Факты о компании ──────────────────────────────────────────────────
    // Всё, что можно проверить, должно быть правдой. null — блок не выводится.
    'facts' => [
        'founded_year'      => null,
        'objects_done'      => null,
        'crews_count'       => null,
        'own_production'    => null,   // true/false — есть ли собственный цех
        'measurement_free'  => true,   // бесплатный замер
        'measurement_terms' => 'Замер бесплатный в пределах бесплатной зоны выезда и не обязывает заключать договор.',
    ],

    // ── Модули и интеграции ───────────────────────────────────────────────
    // Выключенный модуль не ломает сайт: связанные блоки просто не выводятся.
    'features' => [
        'cookie_banner'   => true,
        'analytics'       => false,  // включить после внесения идентификаторов
        'reviews'         => false,  // включить, когда появятся реальные отзывы
        'promotions'      => false,  // включить, когда появятся реальные акции
        'installments'    => false,  // рассрочка — только при реальном договоре с банком
        'certificates'    => false,  // включить после загрузки сканов документов
        'photo_upload'    => false,  // загрузка фото в формах
        'metro_pages'     => false,  // дублирует geo.metro_enabled для маршрутизации
    ],

    // ── Аналитика ─────────────────────────────────────────────────────────
    // Скрипты подключаются ТОЛЬКО после согласия пользователя в cookie-баннере.
    'analytics' => [
        'yandex_metrika_id' => null,   // например 12345678
        'google_id'         => null,   // 'G-XXXXXXX'
        'webvisor'          => false,
    ],

    // ── Служебное ─────────────────────────────────────────────────────────
    'runtime' => [
        // ДЕМОНСТРАЦИОННЫЙ РЕЖИМ (staging).
        // true  — сайт закрыт от индексации ЦЕЛИКОМ:
        //         meta robots = noindex, nofollow на каждой странице,
        //         заголовок X-Robots-Tag, robots.txt с Disallow: /,
        //         пустой sitemap.xml.
        // false — работают обычные правила индексации из реестра страниц.
        //
        // Переключать в false можно только после того, как сайт перенесён
        // на рабочий домен, заполнены реальные данные (DATA_REQUIRED.md)
        // и проведена ручная проверка. Одной этой строкой открывается
        // весь сайт, поэтому меняйте её осознанно.
        'staging' => true,

        // 'production' — посетитель никогда не видит технических ошибок,
        // 'development' — ошибки показываются, включается диагностика конфигурации.
        'env'             => 'production',
        // Диагностическая панель для владельца: показывает незаполненные данные.
        // Доступна по ?diag=КЛЮЧ при непустом diag_key.
        'diag_key'        => null,
        'log_level'       => 'warning',
        // Сколько дней хранить логи и файлы очереди писем.
        'log_retention_days'   => 30,
        'queue_retention_days' => 14,
        // Сколько дней хранить UTM-метки в cookie.
        'utm_lifetime_days'    => 30,
    ],
];

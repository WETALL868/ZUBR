<?php

/**
 * ПРИМЕР настроек базы данных.
 *
 * Скопируйте этот файл рядом под именем database.php и впишите свои доступы.
 * Боевой config/database.php закрыт от скачивания по HTTP (см. .htaccess)
 * и не должен попадать в архивы и в Git.
 *
 *      cp config/database.example.php config/database.php
 */

return [
    // 'mysql' — основной вариант для обычного хостинга.
    // 'sqlite' — если MySQL недоступен: база станет одним файлом на диске,
    //            создавать её на хостинге не нужно.
    'driver' => 'mysql',

    // ---- настройки для MySQL / MariaDB -------------------------------------
    'host'     => 'localhost',
    'port'     => 3306,
    'database' => 'computer',
    'username' => 'computer_user',
    'password' => 'ЗДЕСЬ_ПАРОЛЬ_ОТ_БАЗЫ',
    'charset'  => 'utf8mb4',

    // ---- настройки для SQLite ---------------------------------------------
    // Путь к файлу базы. По умолчанию — вне публичной папки, чтобы базу
    // нельзя было скачать из браузера.
    'sqlite_path' => __DIR__ . '/../storage/database.sqlite',

    // Соль для ссылок предпросмотра черновиков. Замените на свою случайную
    // строку при установке — она не должна совпадать с чужой.
    'preview_secret' => 'ЗАМЕНИТЕ_НА_СЛУЧАЙНУЮ_СТРОКУ',
];
